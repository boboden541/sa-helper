# Semgrep — опциональный движок индексации графа кодовой базы.
# Точнее извлекает классы/функции/эндпоинты и поддерживает Go и C#
# (см. indexer/semgrep_rules/*.yaml). Базовый движок — встроенный Tree-sitter
# (см. requirements.txt) — работает всегда, даже без Semgrep.
#
# Устанавливается командой /project-map на этапе установки зависимостей.
# Если поставить не удалось — индексатор автоматически откатится на Tree-sitter.
semgrep
