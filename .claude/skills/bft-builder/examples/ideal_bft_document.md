# [БФТ] <PROJECT>-<NNN> <Название инициативы>

> Эталон структуры БФТ (нормализовано из корпоративного шаблона Confluence).
> Все `<плейсхолдеры>` заменить. Разделы 1–7 обязательны; раздел 5 (ИТ) может быть «Не применимо» для продуктов без UI.
> Легенда scope (колонка Scope в ФТ): 🟢 — доработка существующего (подтверждено кодом/графом), 🔵 — выход за рамки реализованного, `[ПРЕДПОЛОЖЕНИЕ …]` — scope не подтверждён источниками.

---

## 1 Описание

`<Краткое описание документа: суть и назначение инициативы в 2–4 предложениях. Какую бизнес-потребность закрывает БФТ.>`

---

## 2 Общая информация

| Поле | Значение |
|------|----------|
| Название проекта | `<Название>` |
| Ответственный за продукт | `<ФИО / [не определён]>` |
| Ответственный за документ | `<ФИО>` |
| Платформы | `<web desktop / mobile web / backend / …>` |
| Источник требований | `<PRD / отчёт груминга / свободный текст>` (`<относительный/путь>` при наличии) |
| Статус | Черновик / Ревью / Утверждён |

### 2.1 История изменений

| # | Дата | Автор | Суть изменений |
|---|------|-------|---------------|
| 1 | `<DD.MM.YYYY>` | `<ФИО>` | Создание БФТ на основе `<источник>` |

### 2.2 Дополнительные материалы и артефакты

| Артефакт / Файл / Ссылка | Описание |
|--------------------------|----------|
| `<относительный/путь или URL>` | `<что это>` |
| `sa_documentation/repomix-output.xml` | Источник ограничений системы (если есть) |

---

## 3 Бизнес-требования

### 3.1 Вводные для разрабатываемого функционала

> Вопросы и вводные от бизнес-заказчиков, влияющие на разработку. Открытые вопросы из входа (в т.ч. из отчёта груминга) переносятся сюда с адресатом — не терять.
> Легенда статуса: ✅ подтверждено (будущее требование) · ❌ сделано плохо/не работает · ❓ требует уточнения/без ответа.

| # | Информация или вопрос | Ответ / Статус | Адресат |
|---|-----------------------|----------------|---------|
| 1 | `<вводная или вопрос>` | ✅ / ❌ / ❓ + `<ответ>` | `<PO / архитектор / команда>` |

### 3.2 Ценность разрабатываемого функционала для бизнес-заказчиков

> Корень документа. Каждое требование разделов 4–7 обосновывается пунктом ценности отсюда (колонка «Связанные требования»).

| Идентификатор | Наименование требования | Зависимости | Ценность разрабатываемого функционала для бизнес-заказчиков | Приоритет | Краткое описание доработки | Доп. материалы | Связанные требования |
|---------------|-------------------------|-------------|-------------------------------------------------------------|-----------|----------------------------|----------------|----------------------|
| БТ-`<slug>`-1 | `<наименование бизнес-требования>` | `<команда/система/нет>` | `<какую пользу приносит доработка>` | ВЫСОКИЙ / СРЕДНИЙ / НИЗКИЙ | `<краткое описание доработки>` | `<ссылка/—>` | `<ПТ-…, ФТ-…>` |
| БТ-`<slug>`-2 | `<…>` | `<…>` | `<…>` | `<…>` | `<…>` | `<…>` | `<…>` |

---

## 4 Пользовательские требования

> Story — строго User Story. Одно ПТ — одна атомарная возможность. Колонка «Связанные требования» связывает ПТ с бизнес-требованиями (БТ).

| Идентификатор | Наименование | Story | Комментарий | Связанные требования |
|---------------|--------------|-------|-------------|----------------------|
| ПТ-`<slug>`-1 | `<краткое наименование>` | **Когда я** `<ситуация, как роль>` **Я хочу** `<возможность>` **Чтобы** `<польза>` | `<уточнения, ограничения>` | БТ-`<slug>`-1 |
| ПТ-`<slug>`-2 | `<…>` | **Когда я** `<…>` **Я хочу** `<…>` **Чтобы** `<…>` | `<…>` | БТ-`<slug>`-2 |

---

## 5 Требования к пользовательским интерфейсам

> ТЗ для дизайнера — рекомендательное видение. Что должно быть донесено до пользователя и в каких рамках. Конкретные UX-элементы — на усмотрение дизайнера, но смысл игнорировать нельзя.
> Если продукт без UI — раздел: **Не применимо** (с обоснованием).

| Параметр | Описание |
|----------|----------|
| Тип продукта | `<Web desktop / Mobile web / …>` |
| Продукт | `<название продукта/раздела>` |
| Браузеры | `<Google Chrome, Yandex, …>` |
| Разрешение экрана | `<от 1280px до 1920px>` |
| Вёрстка | `<адаптивная: правила адаптива>` |
| Макеты | `<ссылка на Figma / — >` |
| Видение UX | `<что важно донести до пользователя; направляющие требования к интерфейсу>` |

---

## 6 Функциональные требования

> Что система должна делать. Строки-подзаголовки группируют ФТ по функциональным блокам. Каждое ФТ проверяемо и помечено в колонке Scope. При наличии repomix/графа — с код-опорой в «Параметры, ограничения…»; иначе `[ПРЕДПОЛОЖЕНИЕ …]`.

| Идентификатор | Наименование | Приоритет | Функциональные требования | Параметры, ограничения и связанные требования | Связанные требования | Scope |
|---------------|--------------|-----------|---------------------------|-----------------------------------------------|----------------------|-------|
| **`<Блок 1: напр. Загрузка данных>`** | | | | | | |
| ФТ-`<slug>`-1 | `<наименование>` | ВЫСОКИЙ | Система должна `<что делает>` | `<ограничения; опора: файл:строка / узел графа>` | БТ-`<slug>`-1, ПТ-`<slug>`-1 | 🟢 |
| ФТ-`<slug>`-2 | `<…>` | СРЕДНИЙ | Система должна `<…>` | `<новый функционал — код-опоры нет>` | БТ-`<slug>`-2, ПТ-`<slug>`-2 | 🔵 |
| ФТ-`<slug>`-3 | `<…>` | `<…>` | Система должна `<…>` | `[ПРЕДПОЛОЖЕНИЕ: доработка — подтвердить у <адресат>]` | `<…>` | ❓ |

**Критерии приёмки (примеры):**
1. `<проверяемое условие для ФТ-1>`
2. `<проверяемое условие для ФТ-2>`

---

## 7 Нефункциональные требования

> Измеримые ограничения. Без «резиновых» формулировок — конкретные числа/условия.

| Идентификатор | Наименование требования | Категория | Описание | Измеримый критерий | Связанные требования |
|---------------|-------------------------|-----------|----------|--------------------|----------------------|
| НФТ-`<slug>`-1 | `<краткое наименование>` | Производительность / Доступность / Безопасность / Совместимость | `<что и почему ограничивается>` | `<напр. p95 ≤ 200 мс при 100 RPS>` | БТ-`<slug>`-1 |
| НФТ-`<slug>`-2 | `<…>` | `<категория>` | `<описание>` | `<измеримо>` | БТ-`<slug>`-2 |
