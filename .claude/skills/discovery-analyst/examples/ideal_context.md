# 00_context.md — FNR-<N>

> **Аудитория:** аналитик. **Слой языка:** технический. Здесь уместны `файл:строка`, символы кода, технические термины.

## Что есть (снимок As-Is)
- <Текущее поведение / архитектура — факты из кода с `файл:строка`.>
- <Ключевые компоненты, потоки данных, затрагиваемые сущности/таблицы.>
- <Существующая интеграция / отсутствующая — что уже работает, чего нет.>

## Зоны изменений (где потребуются доработки)
- <Компонент/таблица/интеграция — `файл:строка` — что предположительно меняется.>

## Разрывы / противоречия (питают Decision Backlog)
- <Несоответствие моделей, отсутствующий канал данных, осиротевшая таблица, захардкоженное поведение и т.п. — `файл:строка`.>
- <Каждый разрыв → кандидат в карточку D-N.>

## Белые пятна (`[NEEDS_INVESTIGATION]`)
- <Что мы пока не знаем и не смогли подтвердить кодом.> → идёт в `04_action_points.md` как гипотеза.

## Источники
- repomix-output.xml / живой код / MCP-граф — что использовалось.
- Существующие документы `sa_documentation/` — на что ссылаемся (не дублируем).
