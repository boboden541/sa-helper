# Чек-лист валидации документации

## 1. Фактологическая точность

- [ ] Все ли названия атрибутов в маппинге реально существуют в исходном коде?
- [ ] Соответствуют ли типы данных (String, Numeric, Boolean, Object) реализации в коде?
- [ ] Существует ли указанный эндпоинт/обработчик в конфигурации роутинга проекта?

## 2. Отсутствие галлюцинаций

- [ ] [CRITICAL] Есть ли в тексте утверждения, которые НЕЛЬЗЯ подтвердить кодом? (Если да — исправить или пометить [NEED_CHECK]).
- [ ] Соответствует ли описанный алгоритм реальному потоку управления в методах (без додумывания логики)?

## 3. Соответствие стандартам

- [ ] Соблюдена ли структура согласно целевому шаблону (напр. `ideal_api_document.md`)?
- [ ] [CRITICAL] Для каждого "листа" данных (конечного поля) указано физическое происхождение (Store.Entity.Attribute)?
- [ ] [CRITICAL] **Full Entity Coverage:** Сверены ли ВСЕ поля из программного класса ($schema/DTO/Entity) с таблицей маппинга? Отсутствие полей без обоснования — 🔴.
- [ ] [CRITICAL] **Nested Object Coverage:** Для каждого выходного поля-контейнера (тип `Object<{ObjectName}>` / `Array<{ObjectName}>`) присутствует ли парная подтаблица «Описание вложенного {ObjectName}» (по паттерну из `ideal_api_document.md`)? Нераскрытый вложенный объект — 🔴. *Условие применения:* критерий проверяется ТОЛЬКО при наличии в документе полей-контейнеров объектов; для простых ответов не применяется. *Основание:* срабатывает по тому, что УЖЕ заявлено в документе (поле имеет тип объекта, но не имеет подтаблицы) — реверс-инжиниринг JSON-контракта не требуется, проверяется внутренняя согласованность документа (покрывает и inline-объекты, если их тип проставлен автором). *Граница:* данный критерий проверяет ТОЛЬКО структуру представления вложенных полей (раскрыты ли в подтаблицу); полнота их перечня — в компетенции **Full Entity Coverage**. Один дефект не штрафуется обоими критериями.
- [ ] [CRITICAL] **Typed Container:** Каждое выходное поле-контейнер имеет параметризованный тип с именем элемента/объекта — `Object<{ObjectName}>` (одиночный объект), `Array<{ObjectName}>` (массив объектов) или `Array<{scalar}>` (массив скаляров)? Голый `Object`, `array`, `list` без параметра — нарушение. *Синтаксическая проверка:* критерий смотрит ТОЛЬКО на форму записи типа в документе (есть ли осмысленный параметр в `<...>`), без анализа природы элемента по коду. *Градация:* голый контейнер объектов (у поля есть парная подтаблица «Описание вложенного» или Origin указывает на DTO) — 🔴; голый массив скаляров — 🟡. *Вырожденные параметры:* `Object<>`, `Array<>`, `Array<mixed>` для контейнера объектов — 🔴. *Связь:* критерий предшествует **Nested Object Coverage** — после исправления голого типа на `Object<{ObjectName}>`/`Array<{ObjectName}>` поле штатно подхватывается тем критерием; один дефект → один критерий, без двойного 🔴.

## 4. Проверка инженерной глубины

- [ ] Если данные поступают из внешнего сервиса, указан ли конкретный атрибут из ответа этого сервиса?
- [ ] Описаны ли условия ветвления (`if/else`, `switch`), влияющие на состав выходных данных?
- [ ] Отражены ли в UML-диаграмме вызовы к слою персистентности (напр. `Repository::find`, `Storage::get`)?

## 5. Валидация интерфейса и контракта

- [ ] Описан ли процесс трансформации входящих параметров (напр. из Alias в ID), если он есть в коде?
- [ ] Совпадают ли параметры в документации с сигнатурой метода-обработчика (Handler/Method signature)?
- [ ] Указаны ли значения по умолчанию для необязательных аргументов?

## 6. Автономность и Полнота

- [ ] Позволяет ли описание понять структуру данных в хранилище без открытия кода?
- [ ] Указаны ли физические сущности хранилища (Таблицы/Коллекции) для каждого шага?
- [ ] Содержит ли UML-диаграмма взаимодействия со всеми типами ресурсов (Кэш, БД, Очереди)?

## 7. Проверка соответствия шаблону

- [ ] Соблюдена ли структура из `api_mapping.template.md`?
- [ ] Удалены ли лишние разделы?
- [ ] Совпадают ли заголовки разделов с шаблоном?

## 8. Проверка исключений и метаданных

- [ ] Описано ли поведение метода при отсутствии ресурса или пустом результате?
- [ ] Для вычисляемых (Computed) полей указан слой вычисления [Application / Persistence / SearchEngine]?
- [ ] Отражена ли в логике и UML обработка критических ошибок (Exceptions/Errors), выявленная в коде?

## 9. Качество Диаграмм (PlantUML)

- [ ] Есть ли ошибки в диаграмме? Нет ли ошибок при ее визуализации от uml-server?
- [ ] Все надписи на стрелках выполнены на РУССКОМ языке (бизнес-смысл).
- [ ] Технические методы указаны в скобках.
- [ ] [CRITICAL] Присутствуют блоки `note` с примерами структур данных (JSON/Array/DTO) в местах их передачи. Отсутствие примера — 🔴.

## 10. Качество контента

- [ ] **Semantic Clarity:** Все вычисляемые поля, веса, коэффициенты и статусные ID (enums/flags) снабжены текстовым описанием их назначения («Зачем это полю системе?»).
- [ ] **Infrastructure Traceability:** Все упомянутые системы и базы данных имеют четкие идентификаторы, которые можно соотнести с общей топологией проекта.

## 11. Якоря истниы

- [ ] [CRITICAL] Есть ли заполненный раздел "Якоря истины" со ссылками на конкретные файлы и строки кода?

---

### Hard Gates (Условия СТРОГОГО 🔴)

1. **Hallucination Gate:** Обнаружено хотя бы одно поле или метод, не существующий в коде.
2. **Lineage Gate:** Отсутствует маппинг на физическую таблицу/колонку для любого из выходных полей.
3. **Visualization Gate:** В UML-диаграмме отсутствует блок `note` с примером структуры данных (JSON/Array).
4. **Coverage Gate:** В документе пропущено более 10% полей из исходного программного объекта (DTO/Entity) без явного обоснования их исключения.
5. **Nested Object Gate:** В документе есть поля-контейнеры объектов (тип `Object<{ObjectName}>`/`Array<{ObjectName}>`), систематически не раскрытые в подтаблицы «Описание вложенного {ObjectName}» — 🔴. Срабатывает при СИСТЕМАТИЧЕСКОМ нарушении (наличие полей-контейнеров без подтаблиц как паттерн); одиночные/мелкие упущения остаются замечанием уровня 🟡 в теле критерия «Nested Object Coverage» (раздел 3) и не поднимаются до 🔴.
6. **Typed Container Gate:** Выходное поле-контейнер объектов (у поля есть парная подтаблица «Описание вложенного» или Origin указывает на DTO) записано как голый `Object`/`array`/`list` без параметра типа, либо с вырожденным параметром (`Object<>`, `Array<>`, `Array<mixed>`) — 🔴. Голый массив скаляров — 🟡, до 🔴 не поднимается.
7. **Language Gate:** Надписи на стрелках UML выполнены не на русском языке или содержат только технические имена методов.
8. **Path Gate:** В документе обнаружены абсолютные пути (`file:///`). Все ссылки должны быть относительными.
